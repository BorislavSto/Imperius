using UI;
using UnityEngine;

// Shouldn't be a UIView as it will exist with all other views, disconnected from all.
// And shouldn't be kept track of by the UIManager
public class OptionsView : View 
{
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
